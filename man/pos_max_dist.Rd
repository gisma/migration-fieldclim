% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/position.R
\name{pos_max_dist}
\alias{pos_max_dist}
\title{Maximum Distance between Climate Station and Obstacle}
\usage{
pos_max_dist(dist, obs_width, obs_height, ring = FALSE)
}
\arguments{
\item{dist}{Distance between the climate station and the obstacle (e.g., forest) in meters (m).}

\item{obs_width}{Width of the obstacle in meters (m).}

\item{obs_height}{Height of the obstacle in meters (m).}

\item{ring}{Logical. If TRUE, the obstacle surrounds the station circularly.}
}
\value{
A message indicating if the climate station is well positioned or if it needs to be moved closer to the obstacle.
}
\description{
Checks if the climate station is positioned within the maximum allowable distance from the obstacle.
}
\details{
The maximum distance is calculated to ensure that the climate station's measurements are within a reasonable range from the obstacle. If the station is positioned too far, it needs to be moved closer.

Formula used:
\itemize{
\item For a circularly surrounded obstacle: \eqn{d_{max} = 15 \cdot h}
\item For an obstacle behind the station:
\itemize{
\item If \eqn{h > w}: \eqn{d_{max} = 15 \cdot w}
\item If \eqn{h < w}: \eqn{d_{max} = 15 \cdot h}
}
}
}
\examples{
# Check maximum distance for an obstacle behind the station
pos_max_dist(dist = 500, obs_width = 50, obs_height = 30)

# Check maximum distance for a circularly surrounded obstacle
pos_max_dist(dist = 500, obs_width = 50, obs_height = 30, ring = TRUE)
}
\references{
Bendix 2004, p. 189, eq. 9.1.
}
