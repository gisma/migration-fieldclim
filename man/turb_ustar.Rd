% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/turbulence.R
\name{turb_ustar}
\alias{turb_ustar}
\alias{turb_ustar.default}
\alias{turb_ustar.weather_station}
\title{Friction velocity}
\usage{
turb_ustar(...)

\method{turb_ustar}{default}(v, z, surface_type = NULL, obs_height = NULL, ...)

\method{turb_ustar}{weather_station}(weather_station, obs_height = NULL, ...)
}
\arguments{
\item{...}{Additional arguments.}

\item{v}{Numeric. Windspeed at the height of the anemometer in meters per second (m/s).}

\item{z}{Numeric. Height of the anemometer in meters (m).}

\item{surface_type}{Character. Type of surface. Options: "field", "acre", "lawn", "street", "agriculture", "settlement", "coniferous forest", "deciduous forest", "mixed forest", "city", "water", "shrub".}

\item{obs_height}{Numeric. Height of obstacle in meters (m).}

\item{weather_station}{Object of class \code{weather_station}.}
}
\value{
Numeric. Friction velocity in meters per second (m/s).

Numeric. Friction velocity in meters per second (m/s).
}
\description{
Calculates the friction velocity of the surface.
}
\details{
This function calculates the friction velocity (\eqn{u_*}) of the surface, which is a measure of the shear stress exerted by the wind on the surface. The friction velocity is important in boundary layer meteorology for understanding momentum transfer.

The friction velocity is calculated using the formula:
\deqn{u_* = \frac{v \cdot 0.4}{\log(z / z_0)}}
where \eqn{v} is the windspeed at the height of the anemometer, \eqn{z} is the height of the anemometer, and \eqn{z_0} is the roughness length.

The roughness length (\eqn{z_0}) can be determined based on the obstacle height (\code{obs_height}) or the type of surface (\code{surface_type}).
}
\examples{
# Calculate friction velocity based on obstacle height
turb_ustar(v = 5, z = 10, obs_height = 1)

# Calculate friction velocity based on surface type
turb_ustar(v = 5, z = 10, surface_type = "lawn")

}
\references{
Bendix 2004, p. 239
}
