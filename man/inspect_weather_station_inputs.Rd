% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_inputs.r
\name{inspect_weather_station_inputs}
\alias{inspect_weather_station_inputs}
\title{Inspect weather-station inputs for fieldClim workflows}
\usage{
inspect_weather_station_inputs(
  weather_station,
  targets = c("all", "radiation", "soil", "humidity", "pressure", "profiles",
    "heat_flux"),
  methods = c("all", "priestley_taylor", "bulk_residual", "bowen", "monin_profile",
    "penman")
)
}
\arguments{
\item{weather_station}{Object of class \code{weather_station}.}

\item{targets}{Character vector selecting input groups to inspect. Use
\code{"all"} to include all known groups. Available groups are \code{"radiation"},
\code{"soil"}, \code{"humidity"}, \code{"pressure"}, \code{"profiles"} and \code{"heat_flux"}.}

\item{methods}{Character vector selecting method-readiness checks. Use
\code{"all"} to include all known methods. Available methods are
\code{"priestley_taylor"}, \code{"bulk_residual"}, \code{"bowen"}, \code{"monin_profile"} and
\code{"penman"}. Selecting \code{"bulk_residual"} also includes the optional
\code{"bulk_residual_ri_guard"} readiness check.}
}
\value{
A list of class \code{fieldclim_input_inspection} with fields:
\code{fields}, \code{gaps}, \code{method_readiness}, \code{qc_flags}, \code{guidance}, and
\code{summary}.
}
\description{
Reports available, missing and partially missing inputs for common
\code{fieldClim} method families. This function is read-only: it does not alter,
complete, impute, interpolate, model or replace any value in the supplied
\code{weather_station} object.
}
\details{
The inspection classifies variable groups, missing-value runs and selected
quality-control issues. It is intended to help users decide which downstream
methods cannot run because inputs are missing and which variables need
external quality-control review. Any treatment of missing meteorological
time-series values must happen outside \code{fieldClim} in a documented workflow.

The returned object is a structured inspection report:
\itemize{
\item \code{fields} reports whether known input fields are present, missing or
partially missing.
\item \code{gaps} reports consecutive missing-value runs in present fields.
\item \code{method_readiness} reports whether selected heat-flux workflows have their
required inputs available.
\item \code{qc_flags} reports selected physically suspicious values, such as relative
humidity outside 0..100 percent or negative wind speed.
\item \code{guidance} records the inspection-only policy and the main interpretation
rules.
\item \code{summary} provides compact counts and lists of ready/blocked methods.
}

\code{inspect_weather_station_inputs()} does not decide how missing data should be
treated. The first interpretation step is to classify the affected variable
and the gap length. A one-point gap in a smooth temperature series has a very
different consequence from a long gap in radiation, wind or soil heat flux.
Quality-control problems must also be reviewed before any external
gap-filling workflow is considered.
}
\examples{
dt <- as.POSIXct(
  "2017-06-30 00:00:00",
  tz = "Europe/Berlin"
) + seq(0, by = 300, length.out = 6)

ws <- build_weather_station(
  datetime = dt,
  temp = c(15, 16, NA, 18, 19, 20),
  rh = c(80, 78, 105, 73, NA, 68),
  t1 = c(15, 16, NA, 18, 19, 20),
  t2 = c(14, 15, 16, 17, 18, 19),
  hum1 = c(80, 78, 105, 73, NA, 68),
  hum2 = c(82, 80, 77, 75, 72, 70),
  v1 = c(1.2, -1, 1.1, NA, 1.5, 1.6),
  v2 = c(2.0, 2.1, 2.2, 2.0, 2.3, 2.4),
  rad_bal = c(0, 20, NA, NA, 150, 160),
  soil_flux = c(0, 5, 10, 15, 20, 20),
  lat = 50.8405,
  lon = 8.6832,
  elev = 270,
  z1 = 2,
  z2 = 10,
  surface_type = "field"
)

inspection <- inspect_weather_station_inputs(ws)

names(inspection)
inspection$summary
inspection$fields
inspection$gaps
inspection$qc_flags
inspection$method_readiness

# The function reports missingness and quality-control flags only.
# It does not fill, impute, interpolate, model or replace values.

inspect_weather_station_inputs(
  ws,
  targets = c("radiation", "soil", "profiles"),
  methods = c("priestley_taylor", "bulk_residual", "bowen")
)

}
